{
  "name": "voicebridge-cloud",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "VoiceBridge Phase 1 Cloud Skeleton",
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "npm run build && node --watch dist/src/index.js",
    "start": "node dist/src/index.js",
    "test": "node --test dist/tests/**/*.test.js",
    "check": "npm run build && npm test"
  },
  "devDependencies": {
    "@types/node": "26.1.1",
    "typescript": "7.0.2"
  }
}
